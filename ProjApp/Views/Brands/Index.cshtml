@model IEnumerable<ProjApp.Models.BrandViewModels.BrandViewModel>

@{
    ViewData["Title"] = "Brand List";
}

<h2>Brand</h2>
<h4>List of brands making cars</h4>

<p>
    <a asp-action="Create" class="btn btn-primary btn-sm">New Data</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BrandId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BrandId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BrandId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
